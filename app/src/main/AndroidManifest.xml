<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.uts_tekber">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SyntaxPenyedia"></activity>
        <activity android:name=".SyntaxPenerima" />
        <activity android:name=".SyntaxLayanan" />
        <activity android:name=".SyntaxAktivitas" />
        <activity android:name=".SyntaxAplikasi" />
        <activity android:name=".Aplikasi" />
        <activity android:name=".Penyedia" />
        <activity android:name=".Penerima" />
        <activity android:name=".Layanan" />
        <activity android:name=".Aktivitas" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>